* `monte_1.c` let you have a look of **C standard Library** `stdlib.h`. About defining variable types, macros, functions for performing general functions. 
* `monte_pi` shows a method to compute `pi`.
